 * integrate the simplegeoid library, for example to assert that things which are supposed to be simplegeoids match the regex
 * remove this notion of a "layer"
 * improve code coverage
 * docstrings for API methods
